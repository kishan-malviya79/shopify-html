# Section List

Registry of every implemented section. Check here before starting a new page — reuse an existing section instead of rebuilding it.

| Section (Liquid name) | File | Description | Blocks |
|---|---|---|---|
| `header` | [sections/header.html](sections/header.html) | Site nav bar: left "Products"/"Learn" disclosure buttons (each opens a **full-bleed mega-menu panel** on hover/click — white sheet under the header with a 316px column of big Fredoka links beside a row of `.menu-card` image tiles, and a page scrim behind; see `nav_link` below), centered logo, right action pills — and below 720px a hamburger pill that opens a full-height mobile sheet (link stack + peek-scrolling promo cards + "Shop All") in place of those panels — a yellow "Build Your Bundle" CTA (flat-black pop shadow + floating "Bestseller" ribbon), an outline "Account" pill, and an outline "Cart" pill with a count bubble (hidden at 0 items). On the homepage the dropdown links jump to `#munchief-family`/`#munchief-sodas`/`#combo-bundles` (the 3 `product_carousel` instances), `#our-story` (`brand_story`), and `#reviews` (`testimonial_carousel`) | `nav_link` |
| `announcement_bar` | [sections/announcement-bar.html](sections/announcement-bar.html) | Full-width infinite-scrolling ticker strip (pink bg, dark-maroon text, no border) with sparkle-icon-separated announcement text items — same scroll mechanics as `promo_marquee`, different skin | `announcement_item` |
| `hero_banner` | [sections/hero-banner.html](sections/hero-banner.html) | Full-bleed hero with a swappable image/video background (art-directed product + lifestyle scene), centered "Mouse Memoirs" stroked headline, supporting text, and a pill + icon-circle CTA pair | none |
| `promo_marquee` | [sections/marquee.html](sections/marquee.html) | Full-width infinite-scrolling ticker strip (red bg, no border) with sparkle-icon-separated promo text items | `marquee_item` |
| `world_grid` | [sections/world-grid.html](sections/world-grid.html) | Two-up grid of rounded "world" cards (orange/white-heading + cream/red-outline-heading), each with a bottom-right bleeding product image and bottom-left supporting text | `world_card` |
| `chaos_banner` | [sections/chaos-banner.html](sections/chaos-banner.html) | Full-bleed CTA banner (same media-swap pattern as `hero_banner`): swappable background photo, centered red "Mouse Memoirs" stroked heading + body copy + outline pill/icon-circle "Shop Now" CTA | none |
| `testimonial_carousel` | [sections/testimonial-carousel.html](sections/testimonial-carousel.html) | Cream-bg reviews carousel: centered red "Mouse Memoirs" stroked heading + centered prev/next nav, 3-up grid (1-up scroll-snap on mobile) of bordered rounded testimonial cards — top photo, large star rating, bold title, review body copy, divider, author name | `testimonial` |
| `brand_story` | [sections/brand-story.html](sections/brand-story.html) | Yellow (`#FFD750`) full-bleed "about the brand" section framed top/bottom by a big lazy red wavy rule: intro row pairs a giant red "Mouse Memoirs" stroked "Crunch it. Sip it. Repeat." heading (locks to 233px at desktop) with a rotated gold-bordered hero photo; about row pairs a rotated gold-bordered character-lineup photo with a "Fredoka" ink-brown subheading, body copy, and a pill + icon-circle "About Munchiefam" CTA in a one-off warm-brown (`#5B3419`) skin | none |
| `product_carousel` | [sections/product-carousel.html](sections/product-carousel.html) | Cream-bg product carousel: "Anton" section heading + prev/next nav (`nav_style = "arrows"`) or a "Build Your Bundle"-style pill + icon-circle CTA pair (`nav_style = "cta"`), 3-up grid (1-up scroll-snap on mobile) of product cards — corner badge, star rating, title, sale/compare price + save badge, variant select, full-width red "pop"-shadow Add to Cart. Each card's media/rating/title/price is wrapped in a `.product-card__link` to its PDP (currently all point at `product_main`'s Mimi's Chutney Pop page as a placeholder). Reused 3x on the homepage — snack pouches ("Meet the Munchief Family"), a soda line ("Meet the Munchief Sodas"), and a combo upsell ("Combo. Don't Choose One Mood.", `nav_style = "cta"`, reuses the pouch blocks) — same section/blocks each time, different heading/nav/block content, see index.html |
| `footer` | [sections/footer.html](sections/footer.html) | Yellow (`#FFD750`) full-bleed site footer with a scalloped top edge: münchief wordmark logo (assets/dummy/logo.png, same asset as `header`), two rotated blob-photo callouts bleeding off the giant solid-red "Fredoka" "More Munch / More Crunch" heading, a rotated cream `.newsletter-form` card (navy border, pill input + red pill "Notify Me" button) overlapping the gap between heading lines, a centered 5-column link group, a divider, centered copyright line, and a legal disclaimer strip | `footer_column` |
| `product_story` | [sections/product-story.html](sections/product-story.html) | Terracotta (`#D05835`, matches its Figma dev-mode spec exactly) full-bleed PDP section, cream wavy top edge: 50/50-split info column (heading reuses `.stroke-heading.stroke-heading--black` at `product_carousel`'s existing 82px "Combo" size — Mouse Memoirs, black fill, 5px white stroke — plus Inter/white description + Storage + Ingredients copy, footnote) beside a horizontal peek-scroll `testimonial-card` track (reuses all 3 of `testimonial_carousel`'s exact blocks — Tina A, Sara M, Nina K — no prev/next nav), with a 6-up row of `icon-badge` trust badges below (Less Sugar, Real Ingredients, Supports Digestive Health, a highlighted solid-red "Vegan", Roasted Not Fried, Prebiotic). Not added to index.html — product-detail-page content, not a homepage section. Also rendered directly below `product_main`'s buy box in sections/product-main.html, followed by `product_photo_banner`, so that file previews as one full PDP | `testimonial` (shared), `feature_badge` |
| `flavor_quiz` | [sections/flavor-quiz.html](sections/flavor-quiz.html) | Full-bleed interactive quiz over a swappable illustrated sunburst backdrop (`background_image`, default `assets/dummy/faqbg.png` — rays/halftone dots/stars/squiggles all baked into the photo, same media-swap pattern as `hero_banner`), centered red-fill/white-stroke `.stroke-heading` question heading (96px desktop, matches its own Figma spec — distinct tokens from the shared heading defaults) + supporting text, and a cream gold-bordered card running a 4-step branching flow (progress dots; crunch-or-sip → mood/soda craving → flavour intensity → matched result) that ends on a product image + name + quote + "Shop this" CTA. All 9 possible results (5 snack characters, 4 sodas) render as real block markup the script reads from — nothing is hardcoded in JS. Three transparent character cutouts (`quiz_character_decor` blocks — Boy-LT top-left rotated 180°, Girl-RT top-right, Girl_BL bottom-left) bleed off the section's edges above the backdrop and below the card, `aria-hidden` + `pointer-events: none`; the two flanking the card drop out below 640px. See the header comment for the settings/blocks schema | `quiz_character_result`, `quiz_soda_result`, `quiz_intensity_option`, `quiz_character_decor` |
| `product_main` | [sections/product-main.html](sections/product-main.html) | Standalone PDP layout (not a homepage section — not wired into `index.html`): two-column buy box. Left: main product photo with a floating chat button + a 4-up clickable thumbnail grid that swaps the main image. Right: eyebrow + Anton title, star rating/review-count link, description, sale/compare price + "Save ₹x" badge, tax note, weight/size `.variant-pills` picker (click swaps the Add to Cart button's price), `.qty-stepper` + full-width red Add to Cart button, safe-checkout note, `.pincode-form` serviceability checker, a 4-item `.trust-badges` row (dispatch/payment/replacement/product-claim icons), and a "Must Try" `.upsell-card` cross-sell with its own badge/media/price/CTA. `product_story` (description/ingredients/reviews/trust-badges), `product_photo_banner`, `product_reviews`, `combo_promo`, `faq_section`, a `product_carousel` instance ("Meet the Rest of the Family"), and `footer` all render directly below this in page order, plus `header` above it, so this file previews as the full PDP end-to-end. Mimi's Chutney Pop is the same product already seeded in `product_carousel`. **Pink color scheme:** [sections/product-main-pink.html](sections/product-main-pink.html) is the same PDP for Tantan's Salt Pop with `class="theme-pink"` on `<body>` (plus `announcement_bar` above the header) — that class lives in css/tokens.css and only re-points color tokens, so every section on the page goes pink with zero duplicated section CSS; maps to a `color_scheme` setting in Liquid. Salt Pop cards in `product_carousel`/`collection_grid` link to it, Chutney Pop cards to the default page | `gallery_image`, `variant_option`, `trust_badge` |
| `product_photo_banner` | [sections/product-photo-banner.html](sections/product-photo-banner.html) | Full-bleed PDP photo break: a swappable lifestyle/product image (`image`, dummy default `assets/dummy/pop-bg.png` — macro shot of roasted makhana + green chutney + the Chutney Pop tin/pouch, native ~3:1) framed top and bottom by the same wavy-edge SVG path as `product_story` — top band solid `--color-terracotta` (flat top, wavy bottom dipping into the photo), bottom band solid cream, same path flipped vertically (flat bottom, wavy top rising into the photo). Not added to index.html — PDP visual-break content, not a homepage section (same call as `product_story`). Also rendered on the full PDP in sections/product-main.html | none |
| `product_reviews` | [sections/product-reviews.html](sections/product-reviews.html) | Cream-bg PDP reviews section, `id="reviews"` (the buy box's rating-count link jumps here): centered Anton "Customer Reviews" heading, a white summary card (large `.rating--lg` + "4.75 out of 5" score + review count, a `.review-breakdown` 5-to-1-star histogram, and an outline "Write a review" button), a 3-up grid (1-up on mobile) of `.review-card`s (star rating + "Verified" badge, title, truncated snippet + "Read more", author/date footer), and a `.pagination` row. Not added to index.html — PDP content, not a homepage section. Also rendered on the full PDP in sections/product-main.html | `review` |
| `combo_promo` | [sections/combo-promo.html](sections/combo-promo.html) | Full-bleed combo/bundle offer over a pure-CSS `repeating-conic-gradient` sunburst (no image asset) — centered `.stroke-heading--white` "The Crunch Max Combo" heading over a white `.combo-card` (product media + rating + title + description + sale/compare price + "Save ₹x" badge + `.qty-stepper` + red "Add Combo to Cart" button). Not added to index.html — PDP cross-sell content, not a homepage section. Also rendered on the full PDP in sections/product-main.html (there, the qty-stepper uses distinct `data-combo-qty-*` attributes so its script doesn't collide with `product_main`'s own qty-stepper on the same page) | none |
| `faq_section` | [sections/faq.html](sections/faq.html) (compact) · [sections/faq-page.html](sections/faq-page.html) (page) | One section, two layouts via a `layout` setting. `layout = "compact"` (default, sections/faq.html): two-column PDP FAQ — rotated bordered/shadowed product photo beside a "Questions? We're here to help." Fredoka heading, a 4-item `.accordion` (one row open at a time — click toggles, closes the rest), and a "Still have questions? Let's chat" link. `layout = "page"` (sections/faq-page.html, `.faq--page` modifier): standalone FAQ page — breadcrumb + centered red `.stroke-heading` "Frequently Asked Questions" title above the same grid, flat un-rotated photo with the "Still have questions? / DM us and let's chat." note beneath it, no in-column heading, and a denser 9-row accordion (italic answers, plus-turns-to-minus toggle). Neither layout is in index.html — the compact one also renders on the full PDP in sections/product-main.html; the page one renders announcement_bar + header + chaos_banner + footer around it so the file previews as one full page (same call as product_main / contact_us / collection_grid). Footer "FAQ" links across the site point at sections/faq-page.html | `faq_item` |
| `collection_grid` | [sections/shop-all.html](sections/shop-all.html) | Standalone "Shop All" collection page (not a homepage section, not wired into `index.html` — same call as `product_main`/`contact_us`): left-aligned breadcrumb ("Home > Shop all"), centered Anton "Shop All" title, a centered `.filter-tabs` row (All / Makhanas / Prebiotic Soda / Combos — a small script toggles `hidden` on the groups), then one `.product-grid` per category group under a centered red Fredoka group heading — Makhanas (5 pouches), Munchief Sodas (4 cans), Combos (2 bundles). Product tiles reuse `product_carousel`'s exact `product_card` block markup, wrapping across rows instead of scrolling. `announcement_bar` + `header` render above and `chaos_banner` + `footer` below, so the file previews as one full page. The Combos group isn't in the supplied comp — added so the Combos tab isn't a dead filter | `collection_group`, `product_card` (shared with `product_carousel`) |
| `contact_us` | [sections/contact.html](sections/contact.html) | Standalone Contact Us page (not a homepage section, not wired into `index.html` — same call as `product_main`): breadcrumb ("Home > Contact Us") + centered red `.stroke-heading` "Contact Us" title, above a peach (`--color-contact-card-bg`) rounded card split into a 3-up stack of white `.contact-info__item` cards (Address/Phone/Email, each a coral pin-icon circle + label + value) and a `.contact-form` (First/Last name row, Email, a fused country-code + number `.phone-input`, message textarea, full-width black `.btn--dark.btn--rounded` "Send Message"). Header and footer render around it so the file previews as one full page | `contact_detail` |
| `about_hero` | [sections/about.html](sections/about.html) | First `.band` of the About page: full-bleed character photo with a centred 120px Mouse Memoirs headline (red `--color-about-red` fill, white outline) + one-line subtitle in the photo's open sky, and a rotated red/white "100% Vegetarian" seal. A cream `.about-breadcrumb` strip ("Home > About Us") sits above it | none |
| `about_manifesto` | [sections/about.html](sections/about.html) | Mint (`--color-about-mint`) `.band`: a big packshot bleeding off the left edge, sparkle decor, and a 699px copy column on the right — one 56px Mouse Memoirs sentence that strikes through the claim the brand rejects and flips the promise itself to black, over Comic Neue body copy | none |
| `world_band` | [sections/about.html](sections/about.html) | Full-bleed "world" `.band` where the product scene IS the background photo: a 112px Mouse Memoirs heading, blurb, `.badge--green` ingredient chips and a cream/warm-brown Shop Now pill + icon-circle stacked over its left half. Two instances on the About page — "Crunch World" (white copy) and "Prebiotic World" (red copy with a white outline, `--prebiotic`, last band so it carries the 64px bottom corners). Distinct from `world_grid`, the homepage's two-up card layout | `tag` |
| `character_carousel` | [sections/character-carousel.html](sections/character-carousel.html) | "Real People. Not Cartoons." — cream section with a centred 82px Mouse Memoirs heading + blurb + a 52px prev/next pair, over a peek-scrolling row of 543px `.testimonial-card--character` tiles (tinted peach/pink/butter/gold, white-framed photo, uppercase flavour title, body copy, divider, character name). Distinct from `testimonial_carousel`, the homepage's 3-up grid of cream review cards with ratings. Rendered on the About page between the world bands and `about_moments` | `character` |
| `about_moments` | [sections/about.html](sections/about.html) | Centred red statement heading over a two-column row: a collage of three overlapping `.blob-photo` cloud cutouts on the left, and copy + Shop Now CTA + a star `.rating` on the right | `collage_photo` |
| `bundle_builder` | [sections/build-your-bundle.html](sections/build-your-bundle.html) | Standalone "Build Your Bundle" page (the header's yellow CTA now points here from every page): centered Mouse Memoirs "Build Your Bundle" title + intro copy + a `.filter-tabs` row (All / Makhanas / Prebiotic Soda / Combos), then a 2-column layout — product groups on the left as `.product-card--bundle` tiles with an "+ Add" button (Makhanas 5, Prebiotic Sodas 4, Combos Max Saver 3), and on the right a sticky bundle panel: peach header "Your Mix, Your Rules", step 1 reward milestone bar, step 2 ten slot bars + "N of 10 selected" + the running item list, then subtotal and "Add bundle to cart". The panel reuses `cart_drawer`'s `.cart-rewards*` and `.cart-summary*` (so pages must link cart-drawer.css too); js/bundle.js holds the builder state and hands the finished bundle to the cart via `window.MunchiefCart.add()`. `our_promise` renders directly below it, per the comp | `bundle_group`, `bundle_product`, `reward_milestone` (shared with `cart_drawer`) |
| `our_promise` | [sections/our-promise.html](sections/our-promise.html) | Full-bleed photo band (20% black wash) with a giant red/white `.stroke-heading` "Our Promise" and a row of 4 `.promise-card` sticker cards over the bottom of the photo — cream fill, 2px orange border, flat 10px drop shadow, oversized emoji + title + copy. 900px tall on desktop, 2-up at tablet, peek-scroll on phones. Also rendered below `bundle_builder` on the Build Your Bundle page, which is where its comp places it. Not on index.html | `promise` |
| `cart_drawer` | [sections/cart-drawer.html](sections/cart-drawer.html) | Slide-over cart, built from 3 Figma nodes merged into one section (rule 6): scrim + 473px rounded panel on desktop, full-screen panel below 768px (X swaps to a back chevron, line-item controls stack). Panel = header, a 5-milestone reward progress bar (`reward_milestone` blocks at 10/30/50/70/90% with a red fill showing progress), then either the filled state (line items with `.qty-stepper--outline` + `.select--outline` + price/remove column, coupon row, subtotal/shipping + "Continue to checkout") or the empty state ("Where'd all the Munchiefians go?" + Shop Now, plus a scrollable "Stock up on your favorite flavors" recommendation rail). Line items come from `cart.items` in Liquid, not blocks — here they render from the drawer's `<template data-cart-item-template>` via `js/cart.js`. **Live on every page with a header** (index.html, shop-all, product-main, product-main-pink, contact, legal, faq-page, plus this preview): the header Cart button opens it, every `data-add-to-cart` button adds that card's product and opens it, and quantity/remove/subtotal/reward-progress all recompute from a localStorage cart (the static stand-in for Shopify's Cart AJAX API). Milestone thresholds are ₹299/599/999/1499/1999 via `data-threshold` | `reward_milestone` |
| `legal_page` | [sections/legal.html](sections/legal.html) | Standalone long-form legal/policy document page (not a homepage section, not wired into `index.html` — same call as `contact_us`/`collection_grid`): left-aligned breadcrumb ("Home > Legal Page") + centered red `.stroke-heading.stroke-heading--page` "Legal Page" title, then a 780px-measure document column of 5 policy groups (Hours & Visiting, Reservations & Booking, Cover Charges, VIP Cabanas, Private & Corporate Events) — each a Fredoka group heading + a small "N Questions" meta line + fully-expanded question/answer pairs in Inter. Deliberately not `faq_section`'s `.accordion`: this page keeps every answer open so the whole policy reads (and crawls) in one pass. `announcement_bar` + `header` render above and `footer` below, so the file previews as one full page. Q&A copy is the comp's placeholder venue-policy text (THRöW Social DC), not Munchief's own | `legal_group`, `legal_qa` |

## Liquid conversion — home page

The home page is fully converted under [`Liquid/`](Liquid/): `sections/*.liquid`
(one file per section, own `{% schema %}`), `blocks/*.liquid` (theme blocks),
`layout/theme.liquid`, `templates/index.json` (sections in on-page order), and
`assets/` (tokens.css + components.css global, `section-*.css` per section,
page-wise JS).

Converted sections: `announcement_bar`, `header`, `hero_banner`, `promo_marquee`,
`product_carousel` (backs all 3 home-page instances via settings), `world_grid`,
`brand_story`, `flavor_quiz`, `testimonial_carousel`, `chaos_banner`, `footer`,
`cart_drawer`.

Conventions to follow when converting the remaining pages — see `Liquid/README.md`:
- shared component CSS is global, section CSS is loaded by its own section only;
- JS is imported page-wise by the section that needs it (`defer`), binds every
  instance on the page, and re-inits on `shopify:section:load`;
- merchant-editable values are emitted as scoped `{% style %}` — per section on
  `#shopify-section-{{ section.id }}`, per block on `#block-{{ block.id }}` so
  card 1 and card 2 can carry different styles in the same section.

