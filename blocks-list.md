# Blocks List

Registry of reusable blocks/components so new sections can reuse existing HTML + CSS instead of rebuilding it. Includes both true Shopify blocks (repeatable block types within a section schema) and shared component classes from `css/components.css`.

| Block / component | Used in | Location | Notes |
|---|---|---|---|
| Pill button (`.btn--pill`) | `hero_banner` | css/components.css | Pill shape/sizing only — no color; compose with a skin class. |
| Icon-circle button (`.btn--icon-circle`) | `hero_banner` | css/components.css | Circular shape/sizing only — no color; compose with a skin class. |
| Primary button skin (`.btn--primary`) | `hero_banner` | css/components.css | Cream fill + navy-ink border/label (`--color-cream`, `--color-btn-ink`, `--border-width-btn`). Stacks with `.btn--pill` or `.btn--icon-circle` for shape, e.g. `class="btn btn--pill btn--primary"`. |
| Button group (`.btn-group`) | `hero_banner`, `product_carousel` | css/components.css | Flex row that pairs a pill CTA with a secondary icon button, wraps on small screens. Reused in `product_carousel`'s `nav_style = "cta"` variant with the `.btn--outline` skin (e.g. "Build Your Bundle" + diagonal-arrow icon-circle) in place of the default prev/next arrows. |
| Marquee item (`marquee_item` block, `.marquee__item` + `.marquee__icon`) | `promo_marquee` | css/sections/marquee.css | Repeatable ticker text block; sparkle SVG divider renders after each item. Track is duplicated 6x in HTML (1 real + 5 aria-hidden) and animated -16.6667% (1/6) via `@keyframes marquee-scroll` — 2 copies isn't enough for this short item set to reach the edge of a wide viewport, which left a visible blank gap right before the loop restart; same fix as `announcement_bar`. Predates the shared `.ticker` component below — kept on its own classes/keyframes rather than migrated. |
| Ticker (`.ticker`, `.ticker__track`/`__group`/`__icon`) | `announcement_bar` | css/components.css | Shared infinite-scroll mechanics (flex track, `@keyframes ticker-scroll`, reduced-motion off-switch) factored out for reuse — wrap with a section class for background/border and an item class for text skin instead of re-declaring the scroll animation. `promo_marquee` predates this and still uses its own `.marquee__track` (see row above); point any *new* ticker-style section at `.ticker` instead. |
| Announcement item (`announcement_item` block, `.announcement-bar__item` + `.ticker__icon`) | `announcement_bar` | css/sections/announcement-bar.css | Repeatable ticker text block on the shared `.ticker` mechanics; pink bg + rust-red text (no border), vs. `promo_marquee`'s red bg + white text + blue rule. Text uses its own dense-UI type spec rather than the site's Comic Neue display voice: `--font-ui` ("Inter", 700), `--fs-ticker-item` (12px), `--lh-relaxed` (120%), `--ls-snug` (-0.12px), `--color-rust` (#9a1902) — distinct token set from `promo_marquee`'s `.marquee__item`. |
| Stroked heading (`.stroke-heading`, `.stroke-heading--white`, `.stroke-heading--black`) | `hero_banner`, `world_grid`, `product_carousel`, `product_story` | css/components.css | Display-font headline. Default: red fill + white stroke + drop shadow. `--white`: white fill + coral stroke + shadow (saturated backgrounds). `--black`: black fill + thick white stroke, no shadow — flatter look, used by `product_carousel`'s "Combo" instance and `product_story`'s heading (both at the same 82px `--fs-display-heading-desktop` size). Compose with a section class that sets font-size/line-height. |
| World card (`world_card` block, `.world-card` + `.world-card__media`) | `world_grid` | css/sections/world-grid.css | Repeatable rounded card block: full-bleed photo background (`object-fit: cover`, clipped by the card's `overflow: hidden`) with heading top-left + supporting text bottom-left overlaid on the photo's plain-color side. `--orange`/`--cream` modifiers set the fallback background color shown before the photo loads. |
| Red button skin (`.btn--red`) | `product_carousel` | css/components.css | Solid `--color-brand-red` fill, white label, 2px white border, skeuomorphic `--shadow-btn-pop` bottom edge. Own type spec (Fredoka 600/17px) overriding the base `.btn` (Comic Neue) — this skin's only use case. Darkens to `--color-brand-red-dark` on hover. Stacks with `.btn--pill`/`.btn--block`. |
| Outline button skin (`.btn--outline`) | `product_carousel` | css/components.css | Transparent fill, thin navy-ink border, no shadow. Used for the carousel prev/next `.btn--icon-circle` controls. |
| Block modifier (`.btn--block`) | `product_carousel` | css/components.css | Stretches any `.btn` to 100% width — used for the full-width Add to Cart CTA. |
| Badge (`.badge`, `.badge--green`, `.badge--orange`) | `product_carousel` | css/components.css | Small uppercase pill label, Inter 500. Green skin doubles as both the corner "bought/social-proof" tag and the inline "Save ₹x" tag; orange skin is the "Best Seller" corner tag — both skins share the same dark-ink text color regardless of pill color. |
| Star rating (`.rating`, `.rating__stars`, `.rating__count`) | `product_carousel` | css/components.css | Row of filled star SVGs (`--color-star`) + Inter review-count text. Rating value is currently static markup (5 filled stars); wire to `rating_value`/`rating_count` block settings in Liquid. |
| Price (`.price`, `.price__sale`, `.price__compare`) | `product_carousel` | css/components.css | Inter medium sale price + muted strikethrough compare-at price (same size, both 16px), pairs with a `.badge--green` "Save" pill. |
| Variant select (`.select`, `.select__input`, `.select__icon`) | `product_carousel` | css/components.css | Pill-shaped native `<select>` (Fredoka 500, custom chevron icon overlaid, `appearance: none`) for the variant/price picker. |
| Product card (`product_card` block, `.product-card` + `.product-card__media`/`__badge`/`__title`) | `product_carousel` | css/components.css | Reusable commerce card composing badge + media, rating, title (Fredoka 500/22px), price, select, and a `.btn--red.btn--block` CTA. Reuse for any future product-grid section instead of rebuilding. |
| Large rating modifier (`.rating--lg`) | `testimonial_carousel` | css/components.css | Bumps `.rating__stars` icon size up (14px → 18px) with no count shown — used for review cards where the count doesn't apply. |
| Testimonial card (`testimonial` block, `.testimonial-card` + `__media`/`__body`/`__title`/`__text`/`__divider`/`__author`) | `testimonial_carousel` | css/components.css | Reusable review/UGC card: bordered rounded card, top photo (`aspect-ratio: 4/5`, `object-fit: cover`), then `.rating--lg`, bold title, muted body copy, a divider rule, and author name. Reuse for any future reviews/UGC section instead of rebuilding. |
| Outline button skin (`.btn--outline`) reuse | `chaos_banner` | css/components.css | `.btn--pill btn--outline` + `.btn--icon-circle btn--outline` pair reused for the "Shop Now" CTA, same pattern as `product_carousel`'s `nav_style = "cta"` variant. |
| Newsletter form (`.newsletter-form`, `__input`, `__submit`) | `footer` | css/components.css | Email-capture card: cream (`--color-card-bg`) fill + navy border wrapping a translucent pill `<input>` and a dedicated `.newsletter-form__submit` pill button (Inter 700, `--color-brand-red-bright` fill) — kept separate from `.btn--red`, which is now specialized (Fredoka/bordered) for Add to Cart only. Reuse for any future signup/newsletter section instead of rebuilding. |
| Footer column (`footer_column` block, `.footer__column` + `.footer__column-heading`/`.footer__links`) | `footer` | css/sections/footer.css | Repeatable link group: muted-red (60% opacity) Inter 600 column heading + stacked list of bright-red (`--color-brand-red-bright`) Inter 700 uppercase links. 5 instances on the homepage footer (Drinks, Makhanas, Explore, Munchief, Legal). |
| Blob photo callout (`.footer__bubble`) | `footer` | css/sections/footer.css | Irregular blob-shaped (organic `border-radius`, 240:185 aspect) photo frame with a thick white border, rotated (-15.88deg/+5.14deg) and absolutely positioned to bleed off the footer's oversized heading — stands in for the design's pre-composited footer-cloud-left/right.png cutouts. Hidden below the tablet breakpoint rather than shrunk, to avoid crowding the heading on narrow phones. |
| Nav mega menu (`nav_link` block, `.site-header__nav-item`/`__nav-link`/`__menu`/`__menu-links`/`__menu-group`/`__menu-link`/`__menu-cards`/`__scrim`) | `header` | css/sections/header.css | Repeatable left-nav item: uppercase Fredoka 500 `<button>` (label + chevron that rotates when open) revealing a full-bleed white panel under the header — 48px padding, 96px column gap, a 316px column of 32px Fredoka links (last link pushed to the bottom, e.g. "Shop All") beside a row of `.menu-card` tiles, plus a fixed `.site-header__scrim` over the page. Anchors to `.site-header` (which is `position: relative; z-index: 50`) rather than the nav item, which is why `.site-header__nav-item` is deliberately `position: static`. Opens on `:hover`/`:focus-within` or a click-toggled `.is-open` (js/header.js, which also toggles `.has-menu-open` for the scrim). `visibility` is intentionally left out of the transition — tweening it leaves the panel stuck half-hidden. Replaced the small bordered link dropdown this section originally shipped. 2 instances (Products → 2 links + Shop All + 3 product tiles, Learn → 3 links + 2 wide promo cards). |
| Mobile menu sheet (`.site-header__burger`, `.site-header__mobile-menu` + `__mobile-links`/`__mobile-link`/`__mobile-promo`/`__mobile-cards`) | `header` | css/sections/header.css | Below 720px the mega menus are gone (nav row hidden) and a hamburger pill opens this full-height cream sheet instead: a 18px Fredoka link stack (Makhanas, Prebiotic Sodas, About Us, Contact Us, FAQ's, My Account) up top, and pinned to the bottom a peek-scrolling pair of `.menu-card--wide` promos (2px white border, 16px radius here) over a full-width red "Shop All". Height is `calc(100dvh - 88px)` — the announcement bar + nav row above it. js/header.js toggles `.has-mobile-menu` and locks body scroll. At the same breakpoint the Account pill drops out (the sheet carries "My Account") and the action pills go icon-only, which is the mobile comp's 3-pill row — that icon-only rule used to start at 420px and was raised to 720px when the hamburger joined the row. |
| Menu card (`menu_card` block, `.menu-card` + `__media`/`__badge`/`__title`, `--wide`) | `header` | css/components.css | Tinted rounded photo tile (`--color-tile-pink`/`--lime`/`--rose`, 24px radius) with an optional corner `.badge` and an uppercase Fredoka caption; image scales slightly on hover. `--wide` swaps the square tile for the 454:363 promo crop and a smaller caption. Navigation-only — distinct from `.product-card`, which carries rating/price/variant/CTA. |
| Header behaviour (`js/header.js`) | `header` | js/header.js | Shared by all 9 pages that render the header, replacing the inline copy each carried: click-to-pin a mega menu, `.has-menu-open` for the scrim, outside-click and Escape close (focus returns to the trigger). Second shared JS file, same reasoning as `js/cart.js`. |
| Header action pill (`.site-header__action`, `--bundle` modifier, `.site-header__ribbon`, `.site-header__cart-count`) | `header` | css/sections/header.css | Outline pill button (Fredoka 600, icon + label) for Account/Cart; `--bundle` modifier swaps in a bright-yellow fill + flat black pop shadow + a floating gradient "Bestseller" ribbon (Poppins 500) for the CTA. Cart variant adds an absolutely-positioned count bubble, hidden via opacity at 0 items. Mobile: `header` collapses the left nav (`.site-header__nav`) below 720px rather than adding a hamburger/drawer — revisit if the real nav grows beyond 2 links. Below 420px the 3 action pills also drop to icon-only (visually-hidden text labels, kept for screen readers) — this bug (horizontal overflow on phone-width viewports) predated `product_main`'s full-PDP assembly but had gone unnoticed until that page put the header under an actual narrow-viewport screenshot check; fixed sitewide since `header.css` is shared. |
| Outline button skin (`.btn--outline`) reuse | `brand_story` | css/components.css + css/sections/brand-story.css | `.btn-group` pairing a `.btn--pill.btn--outline` + `.btn--icon-circle.btn--outline` for the "About Munchiefam" CTA — same shape as `hero_banner`/`chaos_banner`'s CTA pairs, but with a section-scoped `.brand-story__cta.btn--outline` override swapping the shared navy (`--color-btn-ink`) border/label for the Figma spec's warm brown (`--color-cta-ink`, `#5B3419`) — same "local skin override" precedent as testimonial-card's monochrome `.rating__stars`. |
| Framed rotated photo (`.brand-story__photo`, `.brand-story__characters`) | `brand_story` | css/sections/brand-story.css | Thick gold-bordered (`--color-photo-border-gold`), drop-shadowed, slightly rotated photo frame (`--radius-photo-frame`) — same "bordered + shadow + rotate" idea as footer's white blob callouts, but plain rounded-rect (15px) instead of an organic blob shape, and gold instead of white. Reuse for any future rotated-photo callout instead of rebuilding. |
| Wavy section rule (`.brand-story::before`/`::after`) | `brand_story` | css/sections/brand-story.css | Hand-drawn sine-wave SVG data-URI, repeat-x, straddling the section's top/bottom edges — same "decorative edge" pattern as footer's scalloped `::before` (css/sections/footer.css) but a wave instead of circles. Point any future wavy-border section at this instead of re-deriving the SVG path. |

| Quiz option pill (`.btn--quiz-option`) | `flavor_quiz` | css/components.css | White-fill/gold-border/rust-text pill skin stacking on `.btn--pill`; reuses `.flavor-quiz__option-icon` (css/sections/flavor-quiz.css) for the step-1 "sip" lightning-bolt icon, colored with the shared `--color-star` gold token. Reuse for any future poll/quiz answer button instead of rebuilding. |
| Small button modifier (`.btn--sm`) | `flavor_quiz` | css/components.css | Compact padding/13px type for a secondary action next to a full-size primary CTA — used for the quiz result's "Try again" pill next to its "Shop this" button. Must be declared after `.btn--pill` to win the padding override. Reuse for any future secondary/tertiary action. |
| Quiz result blocks (`quiz_character_result` / `quiz_soda_result`, `.flavor-quiz__result-data`) | `flavor_quiz` | sections/flavor-quiz.html, css/sections/flavor-quiz.css | Every possible quiz outcome (5 characters + 4 sodas) renders as ordinary hidden block markup (`data-quiz-key`, `data-name`, `data-quote`, `data-cta`, `data-link` + an `<img>`) instead of being hardcoded into the script — the quiz JS only ever copies the matched block's content into the visible `.flavor-quiz__result` slot. Reference build (Downloads/munchief-website 2/munchief-website/index.html#quiz-section) does the equivalent with a hardcoded JS object of base64 images instead; this repo's version keeps result content in editable block markup so it maps cleanly onto Liquid blocks. |

| Icon badge (`feature_badge` block, `.icon-badge` + `__circle`/`__label`, `--solid` modifier) | `product_story` | css/components.css | Outline icon circle + small uppercase Inter label underneath; `--solid` modifier fills the circle with `--color-brand-red` for a highlighted badge (e.g. "Vegan") instead of the default transparent/border-only skin. Reuse for any future trust-badge/feature-icon row instead of rebuilding. |
| Testimonial card (`testimonial` block) reuse | `product_story` | css/components.css | Reuses `testimonial_carousel`'s exact `.testimonial-card` block shape and, in this build, its exact first two blocks' content (Tina A / Sara M) — share block content between the two sections rather than re-authoring it. Rendered in a horizontal peek-scroll track (`.product-story__reviews-track`) instead of `testimonial_carousel`'s centered grid + arrows. |
| Stroked heading (`.stroke-heading--black`) reuse | `product_story` | css/components.css | `.product-story__heading` composes the existing `.stroke-heading.stroke-heading--black` skin (already built for `product_carousel`'s "Combo" heading) at the same `--fs-display-heading-desktop` (82px) size, confirmed against this section's own Figma dev-mode spec (Mouse Memoirs, black fill `#1B1B1B`, 5px white stroke, no shadow) — no new CSS needed. An earlier custom Anton "sticker plaque" treatment (white card, black-stroked white text) was tried first from a screenshot read of this section and dropped once the Figma spec showed the real design just reuses this existing skin; it also triggered a real Chromium rendering bug (rainbow subpixel fringing) for light-fill stroked/shadowed text inside a white bordered box, reproduced with and without GPU acceleration. |
| Wavy section rule (`.product-story::before`) | `product_story` | css/sections/product-story.css | Solid-filled asymmetric cream wave SVG data-URI straddling the section's top edge — same "decorative edge" role as `brand_story`'s stroked-line wave and `footer`'s scalloped circles, but a filled shape instead of either technique. |
| Variant pills (`variant_option` block, `.variant-pills` + `.variant-pill`) | `product_main` | css/components.css | Row of selectable weight/size pills for a PDP buy box (e.g. "70g Tin" / "20g Tasting Pouch"); `.is-selected` swaps to a solid black fill. Distinct from `product_carousel`'s `.select` dropdown — this section's Figma spec uses pills, not a native select. Reuse for any future PDP variant picker. |
| Quantity stepper (`.qty-stepper` + `__btn`/`__value`) | `product_main` | css/components.css | Pill-shaped −/value/+ control paired with the Add to Cart button. Reuse for any future product-quantity control. |
| Pincode form (`.pincode-form` + `__input`/`__submit`) | `product_main` | css/components.css | Inline pill input + solid-black submit button for a delivery-serviceability check. Reuse for any future PDP pincode/serviceability form. |
| Trust badges (`trust_badge` block, `.trust-badges` + `.trust-badge`) | `product_main` | css/components.css | Plain (non-circled) icon-over-label row — distinct from `.icon-badge`'s bordered-circle skin (used by `product_story`'s feature row); this section's Figma spec has bare icons, no circle. Reuse for any future bare icon+label reassurance row. |
| Upsell card (`.upsell-card` + `__badge`/`__media`/`__body`/`__title`/`__text`/`__cta`) | `product_main` | css/components.css | Compact horizontal cross-sell card (overlapping corner badge + media + title/body/price + CTA) for an inline PDP "Must Try"/combo offer. Reuse for any future inline cross-sell card instead of rebuilding. |
| Partial star modifier (`.rating__star--partial`) | `product_main` | css/components.css | Recolors a trailing star to `--color-star-empty` to render a fractional rating (e.g. 4.3 of 5) using the same `.rating__stars` markup as every other section — no separate half-star icon. |
| Product card link (`.product-card__link`) | `product_carousel` | css/components.css | Wraps a `product_card` block's media/rating/title/price in a link to its PDP (`product_main`); the variant `.select` and Add to Cart button stay outside it as siblings on `.product-card`, since interactive controls can't nest inside an `<a>`. Every card currently points at `sections/product-main.html` (Mimi's Chutney Pop) as a placeholder until each product gets its own built PDP. |
| Gallery + thumbnail rail (`gallery_image` block, `.product-main__media`/`__chat`/`__thumbs`/`__thumb`) | `product_main` | css/sections/product-main.css | Main product photo (contain-fit on a cream tile) with a floating circular chat button, plus a 4-up grid of clickable thumbnails below that swap the main image (`.is-active` border marks the current one) — small section script, same pattern as `product_carousel`'s prev/next scroll script. Section-local rather than promoted to components.css since no other section has a gallery yet. |
| Wavy top/bottom photo frame (`.photo-banner::before`/`::after`) | `product_photo_banner` | css/sections/product-photo-banner.css | Reuses `product_story`'s exact wave SVG path (see row above) rather than redrawing it — top band recolors it to `--color-terracotta` (flat top, wavy bottom dipping into the photo); bottom band reuses its original cream fill and flips it vertically via `scaleY(-1)`, same "same path, flip it" precedent as `brand_story`'s `::before`/`::after`. Point any future full-bleed photo-break section at this instead of re-deriving the wave. |
| Review breakdown (`.review-breakdown` + `__row`/`__track`/`__fill`) | `product_reviews` | css/components.css | 5-to-1-star histogram: label + track + solid-black fill bar + count, one row per star value. Reuse for any future ratings-summary block. |
| Review card (`review` block, `.review-card` + `__header`/`__title`/`__text`/`__footer`/`__author`/`__date`) | `product_reviews` | css/components.css | Compact written-review card distinct from `.testimonial-card` (no photo, denser layout: rating + "Verified" `.badge--green`, title, truncated snippet + "Read more" link, author/date footer). Reuse for any future reviews-grid section. |
| Pagination (`.pagination` + `__page`/`__next`) | `product_reviews` | css/components.css | Numbered page pills (`.is-active` = solid black) + a bordered circular next-arrow button. Reuse for any future paginated grid/list. |
| Combo card (`.combo-card` + `__media`/`__body`/`__title`/`__text`/`__cart-row`) | `combo_promo` | css/components.css | Larger promo-card variant of `.product-card`/`.upsell-card` for a dedicated bundle/combo offer: media + rating/title/text/price + `.qty-stepper` + CTA, in a shadowed white card. Reuse for any future standalone combo/bundle section. |
| Sunburst background (`.combo-promo` `repeating-conic-gradient`) | `combo_promo` | css/sections/combo-promo.css | Pure-CSS radiating-rays background (alternating `--color-brand-orange`/`--color-brand-orange-light` wedges) — no image asset needed, scales to any viewport without cropping/blur. Reuse for any future saturated "offer" banner instead of sourcing a sunburst photo. |
| Stroked heading (`.stroke-heading--white`) reuse | `combo_promo` | css/components.css | `.combo-promo__heading.stroke-heading.stroke-heading--white` reuses the existing white-fill/coral-stroke skin (built for saturated backgrounds, e.g. `hero_banner`) for "The Crunch Max Combo" over the sunburst — no new heading skin needed. |
| Accordion (`faq_item` block, `.accordion` + `__item`/`__trigger`/`__panel`) | `faq_section` | css/components.css | FAQ-style disclosure list: bordered rows, each a button trigger (question + rotating plus-to-X icon) revealing an answer panel via a max-height transition; `.is-open` on `.accordion__item` marks the expanded row (script keeps only one open at a time). Reuse for any future FAQ/disclosure-list section. |
| Framed rotated photo (`.faq__photo`) reuse | `faq_section` | css/sections/faq.css | Same "border + shadow + rotate" recipe as `brand_story`'s `.brand-story__photo`, kept section-local rather than promoted to a shared class since `brand_story`'s own version isn't refactored to one yet. |
| Header + footer reuse | `product_main` | sections/product-main.html | The full PDP preview also inlines `header`'s and `footer`'s exact markup/scripts (not just their linked CSS) so the assembled page has real top/bottom chrome — same "separate section, placed here in page order" precedent as `product_story`/`product_photo_banner`, not a block of `product_main`. |
| Header + footer reuse | `contact_us` | sections/contact.html | Same "inline the exact header/footer markup + scripts" precedent as `product_main`, so the Contact Us page previews with real top/bottom chrome instead of just the form section on its own. |
| Breadcrumb (`.breadcrumb` + `__link`/`__sep`/`__current`) | `contact_us` | css/components.css | Plain "Home > Current Page" trail — muted links, bold current-page label. First interior-page nav element in the repo; reuse for any future policy/blog/account page instead of rebuilding. |
| Form field (`.form-field` + `__label`/`__required`/`__input`/`__textarea`, `.form-row`, `.phone-input` + `__code-wrap`/`__code`/`__chevron`/`__number`) | `contact_us` (`contact_detail`/form markup) | css/components.css | Labeled pill text input/textarea system matching contact_us's Figma dev-mode spec exactly (near-black 1px border, `--shadow-xs` edge, pill-radius text inputs, 16px `--radius-textarea` message field) — distinct from `.pincode-form__input`/`.newsletter-form__input` (dark-translucent/plain-white PDP fields). `.form-row` lays first/last name side by side (stacks under 640px); `.phone-input__code-wrap` positions a chevron icon over the appearance:none country `<select>`, same pattern as `.select__icon`. Reuse for any future multi-field form section. |
| Button skin (`.btn--dark`) | `contact_us` | css/components.css | Solid-black fill, white Fredoka 500/16 label, `--shadow-btn-dark` skeuomorphic inset edge — pairs with `.btn--pill` (full stadium shape) for the "Send Message" submit, matching its Figma dev-mode spec exactly. Reuse for any future dense dark form-submit CTA. |
| Contact info card (`contact_detail` block, `.contact-info__item` + `__icon`/`__body`/`__label`/`__value`) | `contact_us` | css/sections/contact.css | White rounded card: a bare 20×20 stroke pin icon (`--color-contact-pin`, no circular backdrop — matches its Figma dev-mode spec exactly) beside a bold label + medium-weight value, `--space-tight` (6px) apart. Section-local since no other section needs this exact layout yet. |
| Filter tabs (`.filter-tabs` + `.filter-tab`, `.is-active`) | `collection_grid` | css/components.css | Row of wrapping toggle pills (Fredoka 500, uppercase, thin near-black border on white) for filtering a grid by category; `.is-active` swaps to a solid black fill — same active-state language as `.variant-pill` and `.pagination__page`. Reuse for any future filter/tab/segmented-control row instead of rebuilding. |
| Product grid (`.product-grid`) | `collection_grid` | css/components.css | Wrapping multi-row grid of `.product-card` tiles for collection/listing pages: 3-up desktop, 2-up ≤1024px and ≤640px (tighter gap), 1-up ≤420px. Distinct from `product_carousel`'s `.product-carousel__track`, which is a single row that becomes a horizontal scroll-snap carousel on mobile — this one keeps wrapping so a long catalog stays fully visible. |
| Product card (`product_card` block) reuse | `collection_grid` | css/components.css | The Shop All page renders `product_carousel`'s exact `product_card` markup (badge/media/rating/title/price inside `.product-card__link`, `.select` + `.btn--red.btn--block` outside it) — no new card CSS. Only the wrapper changes: `.product-grid` instead of `.product-carousel__track`. |
| Collection group (`collection_group` block, `.collection__group` + `__group-heading`) | `collection_grid` | css/sections/shop-all.css | Repeatable category row: centered red Fredoka 600 uppercase heading + one `.product-grid` of `product_card` tiles, with `data-category` matching a `.filter-tab`'s `data-filter` so the filter script can toggle it. Section-local — no other section groups a grid by category yet. |
| Breadcrumb (`.breadcrumb`) reuse | `collection_grid` | css/components.css | Same "Home > Current Page" trail already built for `contact_us`, left-aligned above the page title via a section-local `.collection__breadcrumb { align-self: flex-start }` — same precedent as `.contact__breadcrumb`. |
| Announcement bar + header + chaos banner + footer reuse | `collection_grid` | sections/shop-all.html | The Shop All page inlines `announcement_bar`, `header`, `chaos_banner` and `footer`'s exact markup/scripts so it previews with real page chrome — same "separate section, placed here in page order" precedent as `product_main`/`contact_us`. `chaos_banner` is reused verbatim (background photo, heading, CTA), no new CSS. |
| Accordion (`faq_item` block, `.accordion` + `__item`/`__trigger`/`__panel`, `.is-open`) | `faq_section` (both layouts) | css/components.css | Disclosure list: bordered rows, each a `<button>` trigger (question + plus SVG) revealing a max-height-animated answer panel; a small section script keeps one row open at a time. Default skin rotates the plus 45° into an × when open; `faq_section`'s page layout overrides that to a minus (hides the glyph's vertical `line`) and tightens row padding / italicises answers via `.faq--page` (css/sections/faq.css). Reuse for any future FAQ/disclosure list instead of rebuilding. |
| FAQ page layout (`.faq--page`) | `faq_section` | css/sections/faq.css | Layout modifier, not a new section — swaps the PDP block's in-column Fredoka heading + rotated framed photo for a breadcrumb + centered `.stroke-heading` page title and a flat photo with the "still have questions?" note under it. Same markup, same `faq_item` blocks, same `.accordion` component; maps to a `layout` select setting in Liquid. Same "one section, two layouts" idea as `product_carousel`'s `nav_style`. |
| Announcement bar + header + chaos banner + footer reuse | `faq_section` (page layout) | sections/faq-page.html | The FAQ page inlines the same page chrome as sections/shop-all.html (announcement_bar, header, chaos_banner, footer, plus their dropdown script) verbatim — no new CSS. |
| Stroked heading (`.stroke-heading--page`) | `contact_us`, `legal_page` | css/components.css | Interior-page title skin + size: `--color-brand-red-line` fill, white stroke, no drop shadow, `clamp(32px, 6vw, 82px)` scaling to `--fs-display-heading-desktop` with the thicker desktop stroke at ≥1025px. Started as `contact_us`'s one-off `.contact__heading` override; promoted here when `legal_page` needed the identical treatment, and `contact_us` migrated onto it (its section CSS now only resets the margin). Reuse for any future interior/policy page title. |
| Legal group (`legal_group` block, `.legal__group` + `__group-heading`/`__group-meta`) | `legal_page` | css/sections/legal.css | Repeatable policy section: Fredoka 600 heading + a small muted "N Questions" meta line above its Q&A list. Blocks are a flat list (Shopify blocks can't nest) — every `legal_qa` after a `legal_group` belongs to it until the next group opens. |
| Legal Q&A (`legal_qa` block, `.legal__qa` + `.legal__question`/`.legal__answer`) | `legal_page` | css/sections/legal.css | Always-expanded question/answer pair (Inter 500 question over Inter 400 muted answer at `--lh-legal`). Distinct from `faq_section`'s `.accordion__item`, which hides its answer behind a disclosure toggle — use this one for readable/crawlable policy documents, the accordion for compact FAQ widgets. |
| Breadcrumb (`.breadcrumb`) reuse | `legal_page` | css/components.css | Same trail as `contact_us`/`collection_grid`, left-aligned via a section-local `align-self: flex-start`. |
| Announcement bar + header + footer reuse | `legal_page` | sections/legal.html | Same "inline the exact section markup + scripts for a full-page preview" precedent as `product_main`/`contact_us`/`collection_grid`. The footer's Legal column links now point at this page. |
| Quantity stepper outline skin (`.qty-stepper--outline`) | `cart_drawer` | css/components.css | White fill, 2px `--color-border`, fixed 83px width with the −/value/+ pushed apart — the cart's Figma spec for the same `.qty-stepper` already used by `product_main`/`combo_promo`. Modifier, not a second stepper. |
| Variant select outline skin (`.select--outline`) | `cart_drawer` | css/components.css | Same `.select` component as `product_carousel`, restyled per the cart spec: Inter 13px sentence case (not Fredoka uppercase), 2px `--color-border`, ellipsis on overflow. |
| Cart drawer shell (`.cart-drawer` + `__scrim`/`__panel`/`__header`/`__title`/`__close`/`__back`/`__body`) | `cart_drawer` | css/sections/cart-drawer.css | Scrim + panel overlay: 473px rounded/white-bordered panel pinned top-right on desktop, full-screen with a back chevron below 768px. `.is-open` toggles it; `[hidden]` children are force-hidden since most are flex containers. Reuse for any future slide-over (filters, quick view) instead of rebuilding. |
| Reward progress bar (`reward_milestone` block, `.cart-rewards` + `__text`/`__track`/`__fill`/`__milestone`/`__icon`/`__label`) | `cart_drawer` | css/sections/cart-drawer.css | Horizontal track with a red fill and absolutely-positioned milestone pucks (black circle + exported Figma icon + label) placed by an inline `left: %`. The mobile comp bakes this into a flat PNG; this build keeps it as live CSS so the fill and copy stay data-driven. Labels drop below 480px, icons stay. |
| Cart line item (`.cart-item` + `__media`/`__info`/`__title`/`__meta`/`__controls`/`__aside`/`__price`/`__remove`) | `cart_drawer` | css/sections/cart-drawer.css | Row of photo tile (`--media--yellow` modifier swaps the pink tile for the soda yellow) + title/subtitle + the two shared controls + a right-hand price/remove column. Controls stack under 768px. Not a Shopify block — it renders per `cart.items`. |
| Cart recommendation card (`.cart-rec-card` + `__media`/`__body`/`__title`/`__price`/`__add`) | `cart_drawer` | css/sections/cart-drawer.css | Compact 307px cross-sell card in a scroll-snap rail with prev/next arrows — shown in the empty state only. Denser than `.upsell-card` (`product_main`): no badge, no description, a circular add button instead of a pill CTA. |
| Cart behaviour (`js/cart.js`) | `cart_drawer` (every page with a header) | js/cart.js | First shared JS file in the repo — everything else is a small inline section script, but this one must be byte-identical on 8 pages, so it lives in `js/`. Owns cart state (localStorage), drawer open/close, `data-add-to-cart` → `readProduct()` (reads title/price/image/variant out of `.product-card`, `.cart-rec-card`, `.upsell-card`, `.combo-card` or `.product-main`, so no page needed extra data attributes), line-item render from the drawer's `<template>`, quantity/remove, subtotal, header count bubble, and the reward-bar fill/copy from `data-threshold`. Replace the state layer with /cart/add.js + /cart/change.js in Liquid; the markup contract stays. |
| Cart line-item template (`<template data-cart-item-template>`) | `cart_drawer` | sections/*.html | Keeps the line-item markup in HTML instead of JS string-building — the template's contents map 1:1 onto the `{% for item in cart.items %}` body in Liquid. |
| Figma icon assets (`assets/dummy/icons/*.svg`) | `cart_drawer` | assets/dummy/icons/ | First committed icon assets in the repo — reward tag/shipping/discount pucks, the drawer close X, the recommendation add button and the rail's arrows, exported from Figma rather than hand-drawn (the rest of the repo's icons are inline SVG because equivalent glyphs already existed). Chevrons reuse the existing inline `.select__icon`/nav chevrons. |
| Pink color scheme (`.theme-pink`) | `product_main` (sections/product-main-pink.html) | css/tokens.css | Page-level theme class that only *re-points existing color tokens* — `--color-cream-bg`, `--color-card-bg`, `--color-terracotta`, `--color-wave-cream`, `--color-sunburst-a/b`, `--color-upsell-card-bg/border`, `--color-gallery-thumb-border` — to the pink palette (`--color-pink-hot/soft/pale/card-bg`). Put it on `<body>` (or any wrapper) and every descendant section repaints with no duplicated or overridden section CSS. Brand red (CTAs, prices, footer) stays red by design. Add further schemes the same way — new palette tokens + one class, never a parallel stylesheet. |
| Wavy section edge (`--mask-wave-edge`) | `product_story`, `product_photo_banner` | css/tokens.css | The wave shape as a mask token, applied over a `background-color` (`--color-wave-cream` / `--color-terracotta`) instead of a colored SVG data URI, so the edge recolors with the page theme. Both sections share the one token — `product_photo_banner` reuses it twice (top band, plus a `scaleY(-1)` bottom band). Use this for any future wavy edge instead of pasting a fill-colored data URI. |
| Sunburst rays (`--color-sunburst-a` / `--color-sunburst-b`) | `combo_promo` | css/tokens.css + css/sections/combo-promo.css | Role tokens for the `repeating-conic-gradient` ray pair, defaulting to the warm `--color-brand-orange`/`--color-brand-orange-light` skin. `.theme-pink` swaps them for hot/soft pink without touching the section file. |

`marquee_item` (used in `promo_marquee`) is the first true Shopify block type (repeater content) implemented.
